[package]
name = "ck_lightning_client"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.23.0", features = ["rt", "rt-multi-thread", "macros"] }
ic-lightning-relay = { path = "../ic-lightning-relay" }
cklightning = { path = "../ckLightning-canister" }
# Pinned to match canister + relay crate dependency (candid API compatibility)
candid = "=0.10.19"
anyhow = "1.0.100"
# Pinned to match canister crate dependency (serde_bytes compatibility)
serde = "=1.0.219"
ic-agent = { version = "0.39.3", features = ["wasm-bindgen"] }
clap = { version = "4.5", features = ["derive"] }
ic-ledger-types = "0.15.0"
log = "0.4.29"
env_logger = "0.11.8"
bitcoin = { version = "0.32", features = ["rand"] }
lightning-invoice = "0.33.0"
hex = "0.4.3"

[profile.release]
overflow-checks = true


